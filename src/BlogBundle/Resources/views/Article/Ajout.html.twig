{% extends '@Blog/back.html.twig' %}

{% block title %} Ajouter{% endblock %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block container %}
    <Center>
                    <h4 class="card-title" style="padding-top:150px ">Ajouter  Article </h4>
                    <div class="col-lg-8">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <input type="submit" class="btn btn-primary">
                        {{ form_end(form) }}
                    </div>

    </Center>
{% endblock %}

